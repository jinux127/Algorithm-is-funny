
/**
 * 가장 앞에 서 있는 사람은 비어 있는 심사대로 가서 심사
 * 만약 더 빨리 끝나는 심사대가 잇으면 기다렸다가 그곳으로 가서 심사
 * 최소로 시간
 * @param {number} n 입국심사를 기다리는 사람 수
 * @param {number[]} times 각 심사관이 한명을 심사하는데 걸리는 시간
 * @returns 
 */
function solution(n, times) {
  for(let )
}

console.log(solution(6, [7, 10]));
